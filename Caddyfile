{
    auto_https off
}

:80 {
    # Backend (FastAPI) - Discord interactions + API
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Everything else goes to n8n (editor, webhooks, MCP server)
    handle {
        reverse_proxy n8n:5678
    }
}